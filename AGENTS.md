# Project instructions

Read the applicable project rules before changing code:

- `.agents/rules/code-quality.md` — general code quality and conventions
- `.agents/rules/frontend.md` — frontend implementation rules
- `.agents/rules/backend.md` — backend implementation rules
- `.agents/rules/database.md` — database and data-access rules

Use the applicable workflow from `.agents/workflows/` when the task matches one.

Use skills from `.agents/skills/` when their `description` matches the user's request.
